# Changelog

## [0.0.1] - Initial Release

- Automatic interception of drag-and-drop file moves using `git mv`
- Explicit "Move with Git..." right-click context menu command
- Graceful fallback to VS Code default behaviour when `git mv` fails
- `autoIntercept` and `showNotifications` settings
